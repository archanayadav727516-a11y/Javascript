// function greet(city){
//     console.log(this.name, city);
// }

// const obj = {name: "Archana"};

// greet.call(obj, "Delhi");
// greet.apply(obj, ["Delhi"]);

// const newFn = greet.bind(obj);
// newFn('Delhi');